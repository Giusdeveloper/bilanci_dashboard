/**
 * Barrel del layer di lettura (logica PURA + tipi). Il client supabase-js che
 * consuma questi modelli vive in `client/src/data/financialReads.ts`.
 */
export * from './types';
export * from './financialModel';
